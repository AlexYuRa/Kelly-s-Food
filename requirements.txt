# Dependencias del programa de escritorio Kelly's Food
# Instalar con:  py -m pip install -r requirements.txt
customtkinter>=5.2.0
matplotlib>=3.6.0
openpyxl>=3.1.0

# Ciencia de datos (pronóstico y niveles de demanda del Dashboard).
# Si faltan, el Dashboard funciona igual pero sin esos paneles.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Solo para el backtesting de ml/pronostico.py (SARIMAX); la app no lo usa.
statsmodels>=0.14

# (sqlite3, tkinter y csv vienen incluidos con Python)
